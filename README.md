Установка зависимостей:
npm install

Запуск миграций:
npm run migrate

Запуск сидов:
npm run seed
Можно также выборочно (npm run seed:users, npm run seed:cron)

Запуск сервера:
npm run start

Запуск load тестов:
npm run load_test
